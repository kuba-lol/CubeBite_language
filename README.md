# CubeBite_language
a programming language with very simple syntax
in that moment available only on linux and mac os relesed before mac os catalina(because every mac version before catalina used bash insted of zsh)
